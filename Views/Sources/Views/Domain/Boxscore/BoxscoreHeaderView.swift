//
//  BoxscoreHeaderView.swift
//  mlbapp
//
//  Created by Daniel Weatrowski on 1/25/23.
//

import SwiftUI
/*
struct BoxscoreHeaderView: View {
    
    let viewModel: BoxscoreRowViewModel
    
    var body: some View {
        GridRow {
            Text("Batters - \(teamAbbreviation)")
                .bold()
                .font(.subheadline)

            EmptyGridItem()

            Text("AB")
                .bold()
                .font(.subheadline)
                .foregroundColor(.secondary)

            Text("R")
                .bold()
                .font(.subheadline)
                .foregroundColor(.secondary)

            Text("H")
                .bold()
                .font(.subheadline)
                .foregroundColor(.secondary)
            
            Text("RBI")
                .bold()
                .font(.subheadline)
                .foregroundColor(.secondary)

            Text("BB")
                .bold()
                .font(.subheadline)
                .foregroundColor(.secondary)

            Text("SO")
                .bold()
                .font(.subheadline)
                .foregroundColor(.secondary)

            Text("LOB")
                .bold()
                .font(.subheadline)
                .foregroundColor(.secondary)

            Text("AVG")
                .bold()
                .font(.subheadline)
                .foregroundColor(.secondary)
        }
    }
}

struct BoxscoreHeaderView_Previews: PreviewProvider {
    static var previews: some View {
        Grid {
            BoxscoreHeaderView(teamAbbreviation: "LAD")
        }
    }
}
*/
